Ylanguage: cpp
compiler:
  - gcc
script:  
    make
# script: pwd 
#script: git tag

deploy:
  provider:   releases
  skip_cleanup: true
  api_key:  $GITHUB_TOKEN
  file: hello
  on:
     # tags: "v1.0.2" 
    all_branches: true
script: git tag --sort=committerdate

